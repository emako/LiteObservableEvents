<Window
    x:Class="WpfApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WpfApp"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Grid Margin="10">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button Content="Interactions demo: Request confirmation" Command="{Binding AskConfirmCommand}" Padding="12,6" Margin="0,0,0,8" />
            <TextBlock Text="{Binding LastResult}" HorizontalAlignment="Center" Margin="0,0,0,12" />
            <Button Content="Raise static event + static object event (toggle bool)" Command="{Binding RaiseStaticEventsCommand}" Padding="12,6" Margin="0,0,0,8" />
            <TextBlock HorizontalAlignment="Center" Margin="0,0,0,4">
                <Run Text="Static event toggle: " />
                <Run Text="{Binding StaticEventToggle, Mode=OneWay}" FontWeight="Bold" />
            </TextBlock>
        </StackPanel>
    </Grid>
</Window>
